#negative_margin_header
  #small_logo=image_tag "logo_small.png", :class => "fixpng", :size => '141x25'
  #loginout
    #text_links
      -if user_signed_in?
        !=t(".hola_username", :link => link_to(current_user.get_username, user_root_path))+"&nbsp;&nbsp;&nbsp;|&nbsp;&nbsp;"

        !=link_to(t(".your_public_page"), show_user_path(current_user.get_username))
        &nbsp;


      -if user_signed_in?
        \|
        &nbsp;
        = link_to(t(".settings"), edit_user_path(current_user.id))

      -else
        =link_to t(".login"), new_user_session_path, :style => "margin-right: 10px"
        =link_to(image_tag("facebook_login_small.png", :size => '64x22'), "/auth/facebook", :class => "sth_with_facebook")
        

    -if user_signed_in?
      = link_to(image_tag("btn_logout.png"), destroy_user_session_path, :style => "float: left;")

    -if !user_signed_in?
      =link_to image_tag("btn_registrate.png", :size => '75x22'), new_user_registration_path, :style => "float: left;"
